datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Game {
  id       Int    @id
  name     String
  platform String
}

model Trend {
  id          Int     @id
  word        String
  prediction  Float?
  growthRate  Float?
  acceleration Float?
  platformScore Float?
  aiScore      Float?
  firstSeenAt DateTime?
  platforms   String
}
